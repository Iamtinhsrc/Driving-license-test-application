<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home_graph">

    <include app:graph="@navigation/home_graph" />
    <include app:graph="@navigation/question_graph" />
    <include app:graph="@navigation/category_graph" />
    <include app:graph="@navigation/exam_graph" />
    <include app:graph="@navigation/history_graph" />
    <include app:graph="@navigation/guide_graph" />

    <action
        android:id="@+id/action_global_home"
        app:destination="@id/home_graph"/>

    <action
        android:id="@+id/action_global_examResult"
        app:destination="@id/examResultFragment">
    </action>
</navigation>
